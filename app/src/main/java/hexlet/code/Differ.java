package hexlet.code;

public class Differ {
    public static String generate(String filePath1, String filePath2, String format) throws Exception{
        return "Difference of " + filePath1 + " and " + filePath2 + " in " + format + " format";
    }
}
